package ca.isaaclauzon.abalone.tui

/**
 * A side of the game board's Z-axis.
 */
enum class BoardSide {
    /** The positive Y side. */
    LEFT,
    /** The line of the Z-axis. */
    MIDDLE,
    /** The negative Y side. */
    RIGHT,
}
